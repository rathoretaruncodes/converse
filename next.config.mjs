/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {hostname: "bright-guineapig-212.convex.cloud"},
        ],
    },
};

export default nextConfig;
